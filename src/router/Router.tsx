import { Routes, Route } from "react-router";
const Router = () => {
  return (
    <>
      <Routes>
        <Route path="/" element={<></>} />
      </Routes>
    </>
  );
};

export default Router;
